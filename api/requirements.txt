# Fase 10 §16.5: versiones FIJADAS — un despliegue futuro instala exactamente
# lo mismo que se probó (nada de rangos amplios que cambian comportamiento).
# Para actualizar: sube la versión aquí, corre pytest y despliega.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
PyJWT[crypto]==2.13.0
httpx==0.28.1
pandas==2.3.3
openpyxl==3.1.5
# openpyxl usa el escritor XML acelerado cuando lxml esta disponible. En el
# libro multihoja de estres reduce la primera exportacion auditada sin cambiar
# el contenido ni omitir hojas, formulas o trazabilidad.
lxml==6.1.1
python-multipart==0.0.32
anthropic==0.116.0
